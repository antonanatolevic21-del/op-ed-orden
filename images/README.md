# Резервные изображения

Сюда сохраняются сжатые резервные копии изображений для OP/ED. В Firestore используется относительный путь `images/<имя-файла>.webp` в поле `fallbackImage`.

Основная внешняя ссылка остаётся в `image`; резервная загружается только при ошибке основной.
